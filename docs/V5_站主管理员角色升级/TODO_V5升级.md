# V5_站主管理员角色升级 · 你现在要做的事（TODO_V5升级.md）

> 6A 工作流 · 阶段 6 Assess · 最终输出给用户的直接可操作清单
> 最后更新：2026-07-30（V5.1 新增：私信分「管理员 / 站主」两个独立通道）
> 开发者：代码 & 文档全部完成 ✅  → 你按本清单 1~4 步操作即可

---

## ⚠️ 重要前提（请先读我 ⭐⭐⭐）

- **默认密码体系已生效**：
  - 站主（王鹤澄）首次登录密码 → **123456wHc**（请记好，首次登录后就改成你自己的密码，且只能改 1 次）
  - 其他所有同学默认密码 → **123456**
  - 每位同学（含站主）**仅允许修改 1 次密码**，忘记请联系管理员后台重置（仍可重置）
- **蓝色耀眼框独立系统已生效**：
  - 管理员的「🛡️ 蓝色耀眼框」和同学的「🏅 头衔」完全独立
  - 一个同学可以同时是管理员（蓝色框）+ 头衔"文艺委员"，两个效果同显示
- **V5.1 私信双通道已生效**（本次用户新增需求）：
  - 用户发私信：两个独立按钮「💌 私信管理员」和「👑 私信站主」
  - **管理员收件箱可见范围：**
    - 🛡️ 管理员登录后台 → **仅能看到**同学发给「🛡️ 私信管理员」的消息
    - 👑 站主登录后台 → **两类都能看到**（发给管理员 + 发给站主的全部消息）
  - 同一位同学对两种私信类型会在后台分成**两条独立会话**（studentName 相同，但徽章颜色不同），互不干扰

---

## ✅ 清单 1：立即上线 · 文件推送（一行命令版）

### 你需要 push 到 GitHub 的 **完整文件清单**（V5 所有变更，**全部缺一不可**）：

```
修改文件（5 个，V5.1 新增 direct-messages.js，多 1 个）：
  ✅ netlify/functions/students.js             ← 角色/权限核心后端（迁移+鉴权+5个新接口）
  ✅ netlify/functions/student-login.js        ← 管理员入口角色校验
  ✅ netlify/functions/direct-messages.js      ← 【V5.1 新增】私信双 recipientType (admin/owner) + GET 按角色过滤
  ✅ index.html                                ← 三入口 / 详情蓝金框 / 本人改密码 / 角色管理 Tab / 私信双入口双Tab
  ✅ netlify.toml                              ← 若之前已有，确认存在（通常不用改，但确认 push）

新增文件（0 个 data 文件，因为 V5 是在 GET 接口时自动迁移）
```

### 👉 推荐直接一行命令（复制粘贴到 PowerShell，分支是 `main` 就保持，否则改成你自己的）：

```bash
cd d:\TRAE\cengfan-deploy
git add netlify/functions/students.js netlify/functions/student-login.js netlify/functions/direct-messages.js index.html docs/V5_站主管理员角色升级/*
git status        # （可选）确认上面这些文件 staged 了；如果有其他不要的 unstage
git commit -m "V5.1: 三入口+站主唯一可转让+管理员授撤+改密码1次+蓝框独立+私信分管理员/站主双通道"
git push origin main
```

> Windows 不喜欢命令行就用 SourceTree/GitHub Desktop 勾选上面 **5 个文件** + docs 目录提交。

---

## ✅ 清单 2：Netlify 部署确认

等 GitHub push 完成 → 去 Netlify Dashboard 看最近一次 deploy 状态：

- ✅ 部署成功（绿色）→ 继续
- ❌ 失败（红色）→ 点 deploy 看具体报错日志截图发我

Functions 列表里确认：**students / student-login / direct-messages** 三个函数都有（之前就有，这次只是更新它们的版本）。

---

## ✅ 清单 3：浏览器人工冒烟测试（⭐ 最高优先级，请按顺序做）

> 建议用 Chrome 无痕模式 + 强制刷新 `Ctrl+F5` 开始，避免 CDN 缓存旧的 `index.html`。

### 3.1 基础三入口可用性（3 项）
- [ ] **3.1.1 游客入口 👀** → 回入口页 → 点「👀 游客入口」→ 进主页 → 同学姓名均只显示首字母（如 whc）✅
- [ ] **3.1.2 用户入口 👥** → 回入口页 → 点「👥 用户入口」→ 登录：
  - 姓名：随便一个档案里存在的非管理员同学（例：张三），密码 `123456` → 成功进主页，能看到真实姓名 ✅
  - **同一个张三用「🛡️ 管理员入口」登录 → 红色报错「无权从管理员入口登录」** ✅
- [ ] **3.1.3 管理员入口 🛡️** → 回入口页 → 点「🛡️ 管理员入口」→ **王鹤澄 / 123456wHc** → 成功，直接跳 AdminPage 后台 ✅

### 3.2 角色管理 Tab 授/撤管理员（4 项）
- [ ] **3.2.1 王鹤澄进后台 → Tab 栏能看到「👑 角色管理」** ✅
- [ ] **3.2.2 给 A 同学授管理员** → 在角色管理找到 A → 点「🛡️ 授予管理员」→ 确认 → 绿色成功卡 → A 变「🛡️ 管理员」，启用蓝色框自动勾上 ✅
- [ ] **3.2.3 关掉 A 的蓝色框** → 取消勾选「启用蓝色框」→ 去 A 的详情页：A 仍显示管理员徽章但**无蓝色框** ✅
- [ ] **3.2.4 A + 头衔独立** → 后台把 A 的头衔改成「文艺委员」→ 详情页 A 同时显示「🛡️ 管理员徽章/蓝色框」+「🏅 文艺委员头衔」（两系统独立并存）✅

### 3.3 转让站主（2 项）
- [ ] **3.3.1 转让给 B 同学** → 角色管理找到 B → 点「👑 直接提升为站主」→ 弹窗：完整输入"B 的姓名"二次确认 → 再最后确认 → 成功后：
  - 王鹤澄会变普通同学，顶部「👑 角色管理」Tab 会自动消失并切回 pending ✅
  - **请立即用 B 同学账号管理员入口登录** → 能看到角色管理 Tab ✅
- [ ] **3.3.2 转回到王鹤澄**（为了后续方便建议立即转回）：
  - B 登录 → 角色管理 → 转站主给王鹤澄 → 确认 → 王鹤澄再登录 → 角色管理 Tab 正常 ✅

### 3.4 本人密码修改（2 项）
- [ ] **3.4.1 修改密码（仅一次）**
  - 「👥 用户入口」或从管理员入口登录都行 → 主页面点击任意同学卡片或你自己 → 弹档案 → 「🔐 我的账户」→ 点「🔑 修改密码（1 次）」
  - 旧密码输正确 → 新密码 6~32 位 + 确认相同 → 成功 ✅
- [ ] **3.4.2 再次修改应被拦截**
  - 关弹窗 → 重新打开自己的档案 → 「🔐 我的账户」显示「✅ 已于 XXXX 修改过（限 1 次）」，修改按钮**不显示** ✅
  - 登出重新登录 → 用**新密码**能进、旧密码不行 ✅
  - 忘记密码时 → 找站主/管理员进后台「🔑 密码管理」Tab → 重置 ✅

### 3.5 管理员越权拦截（1 项）
- [ ] **3.5.1 管理员看不到角色管理 Tab**
  - A 同学（管理员身份）管理员入口登录 → AdminPage Tab 栏**没有「👑 角色管理」** ✅
  - A 即便知道接口 URL（手动）→ 后端 requireOwner 也会 403，双保险 ✅

### 3.6 V5.1 私信双通道路由（6 项）
- [ ] **3.6.1 同学端看到两个独立按钮**
  - 用户入口（👥）登录任意同学 → 顶部有 **💌 私信管理员**（蓝灰色）+ **👑 私信站主**（金黄色）两个独立按钮 ✅
- [ ] **3.6.2 「💌 私信管理员」通道**
  - 点击后弹窗标题蓝底 → 发送"我想申请当管理员，请通过一下" → 气泡标签「🛡️ 发给管理员」✅
- [ ] **3.6.3 「👑 私信站主」通道**
  - 再点「👑 私信站主」按钮 → 弹窗自动切换为金色底（提示"仅站主可见，管理员看不到"） → 发送"站主你好，这是给你单独的私信" → 气泡标签「👑 发给站主」✅
  - 弹窗顶部 Tab（🛡️ 管理员 / 👑 站主）切换正常，互不显示对方的历史消息 ✅
- [ ] **3.6.4 管理员（A）后台收件箱**
  - A 管理员入口登录 → 进入「💌 单聊收件箱」Tab
  - **规则校验卡片正确提示**：「🛡️ 你是管理员，仅能看到同学发给「🛡️ 私信管理员」的私信」
  - **左侧会话列表**：只出现「发给管理员」那一条（消息"我想申请当管理员…"），**完全不出现**同学发给站主的会话（黄色「👑 私信站主」会话） ✅
  - **筛选按钮**：左侧只有「全部 / 🛡️ 私信管理员」两个按钮，👑 按钮**不显示**（因为没权限看）✅
- [ ] **3.6.5 站主后台收件箱（站主=王鹤澄）**
  - 站主入口登录 → 进入「💌 单聊收件箱」Tab
  - **规则校验卡片正确提示**：「👑 你是站主，能看到同学发给管理员和发给站主的全部两类私信」
  - **左侧会话列表**：同一位同学出现**两条独立会话**（名字相同，但徽章：一条蓝色「🛡️ 私信管理员」，一条金色「👑 私信站主」）✅
  - 筛选按钮「全部/🛡️ 管理员/👑 站主」三个都显示，切换"只看站主"→ 只显示金色 ✅
  - 分别点两条会话 → 右侧消息区：
    - 点「🛡️ 私信管理员」那条 → 只看到同学发的"我想申请当管理员…"
    - 点「👑 私信站主」那条 → 只看到"站主你好，这是给你单独的私信" ✅
- [ ] **3.6.6 回复带身份标记（站主双身份回复测试）**
  - 站主回复「🛡️ 私信管理员」会话 → 发消息后，**右侧气泡头像显示「管」蓝色**，且气泡名字是「管理员」（因为你以管理员通道回复）✅
  - 站主回复「👑 私信站主」会话 → 发消息后，**右侧气泡头像显示「主」金色**，且气泡名字是「站主」（站主独立身份回复）✅
  - 管理员登录回复 → 只能看管理员通道，发消息头像也是「管」蓝色，看不到金色站主通道 ✅

---

## ✅ 清单 4：缺少的配置 & 推荐优化（不阻塞上线，但建议做）

| # | 待办事项 | 优先级 | 怎么做 / 指引 |
|---|---|---|---|
| 4.1 | ⚙️ **Netlify 环境变量：`STUDENT_TOKEN_SALT`（推荐尽快修改）** | 🔴 高 | 去 Netlify → Site settings → Environment variables → 添加：`STUDENT_TOKEN_SALT` = 你自己的一串长随机字符（如 `whc-2026-蹭饭图-site-owner-secret-xxxx`，**至少 32 位，别用中文**）。保存后触发一次 redeploy → 所有同学需要重新登录。 |
| 4.2 | 🔒 建议**现在就修改王鹤澄的默认密码**（123456wHc → 你自己的 6~32 位密码）| 🔴 高 | 登录用户入口 → 打开自己的档案 → 🔐 我的账户 → 修改。**记得改后写下来或记好，只能改 1 次！** |
| 4.3 | 📣 通知其他同学：默认密码已统一改为「123456」 | 🟡 中 | 在你们的微信群里发一条："大家的登录密码统一改为 123456，第一次登进去后在自己的档案里改自己的密码，只能改 1 次。" |
| 4.4 | 🧪 批量补密码检查（管理员后台 → 🔑 密码管理 Tab）| 🟡 中 | 看统计：所有同学都有密码了吗？如果仍有"缺密码 N 人"→ 点「🗝️ 批量补全密码」→ 然后逐个私下告知新密码。 |
| 4.5 | 👥 指定 2~3 位同学为管理员（按你实际需要） | 🟡 中 | 角色管理 → 授予。例如：班长、副班长、活跃的同学 → 他们可以帮你审批申请、管理公告等。 |

---

## ✅ 清单 5：报错怎么办 → 5 个最常见问题自查

| 现象 | 可能原因 | 你可以立即做什么 |
|---|---|---|
| 入口页白屏 / 红框 JS 报错 | `Ctrl+F5` 还没刷掉旧缓存 / Netlify 还没部署完 | 等 2 分钟再 `Ctrl+Shift+R` 无痕刷新；若仍红框 → **把红框里所有内容截图发我** |
| 管理员入口登录王鹤澄提示「档案中未找到」 | data/students.json 里王鹤澄的 nickname 不是精确的"王鹤澄"三个字（多空格或别名） | 去 GitHub 的 `data/students.json` 搜索看看名字是否真的为"王鹤澄"，不是的话手动改成精确的，保存 commit → 1 分钟后生效。 |
| 管理员入口登录王鹤澄→密码错「123456wHc」 | 大小写敏感，注意 **w 小写、H 大写、c 小写**；或者你之前已改密码了（用改后的新密码登；忘记就用「🔧 超级管理员密码登录（备用）」进后台重置自己） |
| 改密码后按钮仍显示 / 或仍能改 | 后端没存成功，可能网络或 GITHUB_TOKEN 权限不足 | 点 F12 → Network 面板 → 找到 change-password 的 POST 请求，把 Response 内容截图发我 |
| 转让站主后原站主仍能看到角色管理 Tab | 原站主同学会话 token 未变（token 是前端态）→ 让原站主登出再登录，或刷新页面（会用新角色态） |

---

## 📞 做完后告诉我结果

把**清单 3 的 12 项**结果（打勾/截图）和你选择的清单 4（缺失配置处理方式）反馈给我：
- 成功的项 → 告诉我「3.x 通过」
- 失败的项 → 发截图 + 红框/控制台 Network 面板 Response 内容

我根据失败项直接打补丁修复。✅
